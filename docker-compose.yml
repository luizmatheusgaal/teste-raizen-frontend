version: '3.9'

services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: http://localhost:8000/api/v1
    container_name: verzel-frontend
    ports:
      - "5173:80"
    environment:
      - NODE_ENV=production
